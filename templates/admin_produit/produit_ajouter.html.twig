{% extends 'base.html.twig' %} 

{% block title %}{{ parent() }} | Back Office | Gestion des produits | Ajouter{% endblock %}

{% block titreh1 %}Ajouter un produit{% endblock %}



{% block body %}

    <div class="col-md-6 mx-auto text-center">



        {# {{ form(formProduit) }}
        la fonction twig form() permet d'afficher l'intégralité d'un formulaire : c'est-à-dire, de la balise HTML form ouvrante à la fermante 
        Ne pas oublier de placer le bouton submit dans ProduitType #}



        {{ form_start(formProduit)  }} 
        {# la fonction form_start ouvre le formulaire c'est l'équivalant de la balise form ouvrante #}

            <div class="row">



                <div class="col-md-6">
                    {{  form_row(formProduit.nom ,  { 
                            'attr' :  { 
                                "class" : "border border-info"
                            }     
                        })      
                    }}

                    {# en twig les tableaux se placent entre accolade { }
                       L'affectation d'une valeur est : 
                     #}




                </div>

                <div class="col-md-6">
                    {{ form_row(formProduit.prix) }} 
                    {# la fonction form_row permet d'appeler un input
                    Si l'input n'est pas appelé dans le twig mais existant dans ProduitType, il sera affiché tout à la fin  #}
                    
                </div>

            </div>

            <div class="col-md-12">

                    {{ form_row(formProduit.categorie) }} 
                    
            </div>

            {{ form_row(formProduit.image , {

                    'attr' : {
                        "onChange" : "loadFile(event)"
                    }
                }) 
            }}

            <div class="col-md-12 text-center">
            
                <img id="image" style="width:300px" alt="">
            
            </div>




            <div class="row justify-content-between">
                <a class="btn btn-info col-md-5 mt-3 text-white" href="{{ path('produit_afficher')}}">Gestion des produits</a>
                <input type="submit" value="Ajouter" class="btn btn-success col-md-5 mt-3">
                {# on peut y placer notre input de type submit, tant qu'il est à l'intérieur de la balise form #}
            </div>


        {{ form_end(formProduit)  }} 
        {# la fonction form_end ouvre le formulaire c'est l'équivalant de la balise form fermante #}

    </div>



{% endblock %}

{% block javascripts %}

    <script>
    

        var loadFile = function(event)
        {
            var image = document.getElementById('image');

            image.src = URL.createObjectURL(event.target.files[0]);
        };

    
    </script>

{% endblock %}

